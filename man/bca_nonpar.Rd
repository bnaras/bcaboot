% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bca_nonpar.R
\name{bca_nonpar}
\alias{bca_nonpar}
\title{Nonparametric BCa bootstrap confidence intervals}
\usage{
bca_nonpar(
  x,
  B,
  func,
  ...,
  accel = c("regression", "jackknife"),
  conf.level = c(0.95, 0.9, 0.8, 0.68),
  n_groups = nrow(x),
  group_reps = 5,
  kl_fraction = 0.333,
  n_jack = 2,
  jack_groups = 12,
  boot_data = NULL,
  verbose = TRUE
)
}
\arguments{
\item{x}{An \eqn{n \times p} data matrix. Rows are observed
\eqn{p}-vectors, assumed independently sampled. If \eqn{p = 1}
then \code{x} can be a vector.}

\item{B}{Number of bootstrap replications (integer).}

\item{func}{Function computing the parameter of interest.
\code{func(x, ...)} should return a scalar for any data matrix.}

\item{...}{Additional arguments passed to \code{func}.}

\item{accel}{Method for estimating acceleration:
\code{"regression"} (default) uses local regression on bootstrap count
vectors nearest to uniform; \code{"jackknife"} uses classical
delete-one (or delete-group) jackknife influence values.}

\item{conf.level}{Confidence levels for the intervals (e.g.,
\code{c(0.95, 0.90, 0.80, 0.68)}). Only values in \verb{(0, 1)} are used.}

\item{n_groups}{Number of groups for jackknife. Default \code{nrow(x)}
gives delete-one jackknife. Set smaller (e.g., 20-40) to speed up
computation on large datasets.}

\item{group_reps}{When \code{n_groups < nrow(x)}, number of random
grouping repetitions to average over. Only used with
\code{accel = "jackknife"}.}

\item{kl_fraction}{Fraction of bootstrap count vectors nearest to
uniform used in the local regression. Only used with
\code{accel = "regression"}. Default 1/3.}

\item{n_jack}{Number of delete-d jackknife repetitions for estimating
internal (Monte Carlo) standard error. Set to 0 to skip.}

\item{jack_groups}{Number of groups per jackknife fold.}

\item{boot_data}{Optional pre-computed bootstrap data: a list with
components \code{Y} (B x n count matrix), \code{tt} (bootstrap replicates),
\code{t0} (original estimate). When provided, \code{B} is ignored and no
resampling is performed.}

\item{verbose}{Logical; show progress bar during bootstrap.}
}
\value{
An object of class \code{"bcaboot"} with components:
\describe{
\item{limits}{9-row matrix of confidence limits (columns: \code{bca},
\code{jacksd}, \code{std}, \code{pct})}
\item{stats}{2-row matrix of estimates and jackknife SEs (columns:
\code{theta}, \code{sdboot}, \code{z0}, \code{a}, \code{sdjack})}
\item{B_mean}{Bootstrap sample size and mean of replicates}
\item{ustats}{Bias-corrected estimator and its SE}
\item{diagnostic}{gbca diagnostic (when \code{accel = "regression"})}
}
}
\description{
Computes nonparametric bias-corrected and accelerated (BCa) bootstrap
confidence intervals. Consolidates the functionality of the deprecated
\code{bcajack}, \code{bcajack2}, and \code{bcanon} functions.
}
\examples{
data(diabetes, package = "bcaboot")
Xy <- cbind(diabetes$x, diabetes$y)
rfun <- function(Xy) {
  y <- Xy[, 11]
  X <- Xy[, 1:10]
  summary(lm(y ~ X))$adj.r.squared
}
set.seed(1234)
result <- bca_nonpar(x = Xy, B = 1000, func = rfun, n_groups = 34,
                     verbose = FALSE)
print(result)

}
\references{
Efron B (1987). Better bootstrap confidence intervals.
JASA 82, 171-200.

Efron B and Narasimhan B (2020). Automatic construction
of bootstrap confidence intervals. JASA 115, 1895-1905.
}
