% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bca_par.R
\name{bca_par}
\alias{bca_par}
\title{Parametric BCa bootstrap confidence intervals}
\usage{
bca_par(
  t0,
  tt,
  bb,
  conf.level = c(0.95, 0.9, 0.8, 0.68),
  n_jack = 6,
  jack_groups = 10,
  truncation = 0.001,
  kl_fraction = 0.333,
  conf_density = FALSE,
  func
)
}
\arguments{
\item{t0}{Observed estimate of theta, usually by maximum likelihood.}

\item{tt}{A vector of B parametric bootstrap replications of theta.}

\item{bb}{A B by p matrix of natural sufficient vectors, where p is
the dimension of the exponential family.}

\item{conf.level}{Confidence levels for the intervals.}

\item{n_jack}{Number of jackknife repetitions for internal SE.
Set to 0 to skip.}

\item{jack_groups}{Number of groups per jackknife fold.}

\item{truncation}{Truncation parameter for acceleration calculation.
Can be a vector to compute acceleration at multiple levels.}

\item{kl_fraction}{Proportion of "nearby" b vectors used in
gradient estimation.}

\item{conf_density}{Logical; if TRUE, return BCa confidence density
weights.}

\item{func}{Optional function for ABC (analytical bootstrap) limits.}
}
\value{
An object of class \code{"bcaboot"} with components:
\describe{
\item{limits}{9-row matrix of confidence limits}
\item{stats}{2-row matrix of estimates and jackknife SEs}
\item{B_mean}{Bootstrap sample size and mean of replicates}
\item{ustats}{Bias-corrected estimator and its SE}
\item{abc}{ABC limits and stats (when \code{func} provided)}
\item{conf_density}{Confidence density weights (when requested)}
\item{accel_matrix}{Acceleration at multiple truncation levels}
}
}
\description{
Computes parametric bootstrap confidence intervals for a real-valued
parameter theta in a p-parameter exponential family.
}
\examples{
data(diabetes, package = "bcaboot")
X <- diabetes$x
y <- scale(diabetes$y, center = TRUE, scale = FALSE)
lm.model <- lm(y ~ X - 1)
mu.hat <- lm.model$fitted.values
sigma.hat <- stats::sd(lm.model$residuals)
t0 <- summary(lm.model)$adj.r.squared
y.star <- sapply(mu.hat, rnorm, n = 500, sd = sigma.hat)
tt <- apply(y.star, 1, function(y) summary(lm(y ~ X - 1))$adj.r.squared)
b.star <- y.star \%*\% X
set.seed(1234)
bca_par(t0 = t0, tt = tt, bb = b.star)

}
\references{
Efron B (1987). Better bootstrap confidence intervals.
JASA 82, 171-200.

DiCiccio T and Efron B (1992). More accurate confidence
intervals in exponential families. Biometrika, 231-245.

Efron B and Narasimhan B (2020). Automatic construction
of bootstrap confidence intervals. JASA 115, 1895-1905.
}
