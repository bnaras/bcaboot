% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.bcaboot}
\alias{tidy.bcaboot}
\title{Tidy a bcaboot result}
\usage{
\method{tidy}{bcaboot}(x, ...)
}
\arguments{
\item{x}{A \code{bcaboot} object from \code{\link[=bca_nonpar]{bca_nonpar()}} or \code{\link[=bca_par]{bca_par()}}.}

\item{...}{Ignored.}
}
\value{
A tibble with columns:
\describe{
\item{conf.level}{Confidence level (e.g. 0.95)}
\item{method}{\code{"bca"} or \code{"standard"} (or \code{"abc"} when available)}
\item{estimate}{Point estimate (theta)}
\item{conf.low}{Lower confidence limit}
\item{conf.high}{Upper confidence limit}
\item{jacksd.low}{Jackknife SE of lower limit (BCa only; \code{NA} otherwise)}
\item{jacksd.high}{Jackknife SE of upper limit (BCa only; \code{NA} otherwise)}
}
}
\description{
Returns a tibble with one row per (confidence level, method)
combination, following broom conventions.
}
\examples{
data(diabetes, package = "bcaboot")
Xy <- cbind(diabetes$x, diabetes$y)
rfun <- function(Xy) {
  y <- Xy[, 11]; X <- Xy[, 1:10]
  summary(lm(y ~ X))$adj.r.squared
}
set.seed(1234)
result <- bca_nonpar(Xy, 1000, rfun, n_groups = 34, verbose = FALSE)
tidy(result)

}
